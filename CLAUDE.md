# deadhangs-com — Project Brain

Per-repo brain, migrated from central claude-memory 2026-06-20. Canonical project memory now lives here.

## Current state

deadhangs.com — dead-hang fitness content + affiliate site. **Rebuilt May 9 2026 on Astro 5 + Tailwind 4** (cyan-accent manifesto design), replacing the older static HTML site. ~53 pages. Repo `sunnyp81/deadhangs-com`, local `C:\Users\sunny\repos\deadhangs-com`. Auto-deploys from git push to Cloudflare Pages (project `deadhangs-com` → `deadhangs-com-bew.pages.dev` → `deadhangs.com`). Revenue £0 (AdSense application pending; Amazon affiliate tag `deadhangs-21`).

Monetisation: Amazon affiliate (pull-up bars, chalk) + a Brevo free-tier 12-week drip email campaign + AdSense (not yet applied). Interactive tools: Dead Hang Calculator (age × gender × seconds → percentile) and HangTimer (Simple/EMOM/Ladder modes).

**Top traffic page:** `/dead-hang-time-by-age/` (was ~21.7k impr, pos 7). `/dead-hang-world-record/` is the other big one.

## Key facts & warnings

- 🔴 **Separate Cloudflare account** from main sunnypat81: CF account `fiedss47hh637@gmail.com`, account ID `d2373b6986cd74e0f99d4927b57e8d46`, project `deadhangs-com`. (Now auto-deploys from git push.) If a manual wrangler deploy ever breaks with an auth error, the sunnypat81 wrangler OAuth session overrides the env var — the old fix was correcting `.wrangler/cache/pages.json` account_id. CF API token + StaticForms accessKey + Brevo API key live in central memory / CF Pages secrets — pointers only, never copy.
- 🔴 **All fabricated authority signals were removed (May 9) — keep them gone.** No Berkeley CA, no "study N=2847", no "our cohort", no version numbers, no "Made in California". Every stat must cite a real paper: Leong/Lancet 2015 (grip strength + mortality), Bohannon 2019, Kirsch 2017, Peterson 2023. Dead-hang world record = **80 min 41 sec by Kenta Adachi (Japan), 18 Nov 2022** (verified GWR — an earlier version wrongly showed 16:03 Harald Riise).
- Brevo drip: account `hello@deadhangs.com`, contact list ID 3 ("Dead Hang Protocol"), 12 HTML templates (IDs 1-12) in Brevo. Drip logic in `functions/api/drip-cron.ts`; `deadhangs-drip` Worker cron 09:00 UTC daily calls it; `/api/subscribe` adds contacts. Template content also in `emails/drip-sequence.md` + `emails/drip-api.json`. Buttondown account exists but unused (automations need paid plan).
- No em/en dashes in content (global rule).
- New-page conventions (from May 10 audit): use `Article.astro` layout (auto-canonical, auto-BreadcrumbList, cookie banner in layout). Titles <60ch, no `| DeadHangs.com` suffix. FAQ = `<div class="faq">` + `<h3>` pattern, gets FAQPage schema. Org + WebSite + BreadcrumbList schema is in Base.astro/Article.astro.
- Legacy CSS-bug lessons (older HTML era, but still good practice): inline SVGs with no width/height default to 300×150px — always set explicit size. Article-level elements (`.author-box`, `.sources`) outside the inner container need their own `max-width:720px; margin:auto`.

## History

- 2026-03-23 — Older static HTML era: white editorial design ("Instrument Serif" + "Plus Jakarta Sans", teal #0d9488), StaticForms email capture mid-article. Deployed from a git repo on Drive; UX redesign (hamburger nav, hero, hub cards).
- 2026-04-01 — Big content + design overhaul (dark "Chalk & Iron" theme, brand #ea580c): 8 key pages rewritten with real UGC (StrongFirst, PURE/Lancet, Peterson 2023), CTR title rewrites, world-record fact correction (Kenta Adachi). Fixed CSS bugs (oversized SVG, page-header block, unstyled meta/author/sources).
- 2026-04-23 — Built Dead Hang Calculator + contact page (StaticForms), ads.txt placeholder, noindexed 4 thin community stubs.
- 2026-05-09 — Full rebuild on Astro 5 + Tailwind 4; Brevo drip system; all fabricated authority signals stripped.
- 2026-05-10 — Full SEO audit + fixes (53 pages, commits e7b9138/cdff1d7/1118640/1db12aa/1db...): global Org/WebSite/BreadcrumbList schema, canonicals + OG, @astrojs/sitemap, llms.txt/ai.txt, 49 titles shortened, FAQ/Article schema added, cookie banner moved to layout, cannibalization pairs differentiated, HangTimer 3-mode + ProtocolCalculator wired.

Open / next: fill ads.txt with AdSense publisher ID then apply; build 2-3 backlinks to `/dead-hang-time-by-age/` (pos 7 → 4-5 doubles CTR); homepage authority for "deadhangs" brand term.

## Brain

- 2026-07-19 (final round, post-Fable-red-team) — **Self-correction + indexation fixes, shipped LIVE** (commit 727fe03, verified live).
  - 🔴 **BIGGEST FINDING: roughly half the site has never been crawled.** GSC batch inspection of 20 key URLs returned **11 NOT indexed**, incl. `/dead-hang-calculator/` (the main interactive asset), `/training-programs/`, `/how-to-deadhang/`, `/about/`, `/best-pullup-bars/`, `/spine-decompression/`, `/shoulder-health/`, `/4-week-program/`, `/longevity-research/`, `/deadhang-workout/`, `/dead-hang-standards/` (the last was literally "URL is unknown to Google"). Ruled out technical causes: **zero `noindex` in dist, no `Disallow` in robots.txt, all 53 URLs in the sitemap.** Diagnosis = crawl budget + orphaning (Footer/Nav were dead `href="#"` until this date, stale 85-URL sitemap frozen at 2026-03-03, ~zero backlinks). **The site's ceiling is indexation + authority, NOT content/CTR.**
  - **Corrected two factual errors I had introduced** on the world-record page (Fable's red-team caught them): Riise's 16:03 was set **7 November 2020**, not 2021 (I'd keyed off the June-2021 GWR announcement); and the Guinness category is **"Longest duration in the dead hang position (male)"**, not an "open category". Both verified on guinnessworldrecords.com.
  - **Women's section rebuilt on verified facts** (was hedged prose pulling 15,910 impr at pos 6.1, 0 clicks): Guinness lists no open female duration record; the verified female title is "Oldest person to hold the dead hang position (female)" — **Ann Crile Esselstyn (USA), 2 min 52 sec at 90 years 231 days, 6 Mar 2026**, taking it from **Annie Judis (USA, 81, 2024)**. Table + 3 GWR source lines added. This is also the site's best link-outreach hook.
  - **Fixed a SITEWIDE SOFT-404:** there was no `404.html`, so Cloudflare Pages served **200 + homepage HTML for every non-existent URL**, including all 54 deleted legacy URLs. Added `src/pages/404.astro`. Verified live: bogus URLs and `/dead-hang-standards.html` now return real 404s.
  - **Sitemap plumbing:** the stale `sitemap.xml` was still registered in **both GSC and Bing** (85 URLs, last downloaded 2026-07-12) even after repo deletion. Removed from both. Submitted the 11 unindexed URLs to Bing (quota 96/day).
  - **Page realignment per the red-team:** `/dead-hang-standards/` retitled "Dead Hang Standards: Beginner to Elite Benchmarks" (matches its H1; it does NOT own the "good dead hang time" cluster and had zero impressions ever) and its verdict table + "is X good" FAQs were MOVED to `/dead-hang-time-by-age/`, which actually ranks pos 8-12 for that cluster. Fixed a number contradiction there (men "40-60" → "30 to 60 seconds"). `/deadhang-guide/` retitled to include "Workout Plan" + gained a workout-plan table, because **it** (not `/deadhang-workout/`) ranks pos 11.6 for "dead hangs workout plan" (2,677 impr).
  - **Strategy correction from Fable's red-team:** DROP `/dead-hang-norms/` permanently (cannibalises the age page). On-page edits move CTR at current position; they will NOT move pos 7 → 4 — that needs **links**. Sequence links and indexation BEFORE new pages.
  - **OPEN / blocked on Sunny:** per-URL 301 approval (`/deadhang-workout/`→guide; merge one-arm and weighted duplicate pairs; 301+desitemap the thin "dead hangs for [audience]" pSEO stubs); real AdSense publisher ID for `ads.txt` (still `pub-XXXX`); manual "Request Indexing" in GSC for the calculator/standards/training-programs + the 3 changed money pages (no API for it — Google still shows the old wrong world-record title in SERPs); decision on a real named author for E-E-A-T now that fake credentials are gone.
- 2026-07-19 (later, "fix it + optimise it") — **Authority/quality cleanup + schema optimise, shipped LIVE** (commits b3fa57d + 7afd5d1, all verified live).
  - **Fixed the fabricated authority (P3-1, was the parked owner-decision):** removed "NSCA-CSCS & NASM-CPT Certified / certified personal trainers & physical therapists / 10+ years / Expert Reviewed" from all 48 author boxes (sitewide Node script). Rewrote `/about/` honestly as "an independent publisher... not a medical provider... nothing is a substitute for professional medical advice", citing the real Leong/Bohannon/Kirsch sources (removed fake team, credentials list, "clinical review by licensed PT", invented journals + grip-sport-competitor claims). Verified live: 0 residual.
  - **Removed uncited stats:** the fabricated "25% in 4 weeks recreational-climber study" (deadhang-benefits, deadhang-guide, grip-strength) and the unsourced "40-60% disc pressure" figure (deadhang-guide x3). Kept the real Leong/Lancet 17% mortality stat.
  - **Sitewide hygiene:** purged 218 mojibake sequences + 107 real em/en dashes across all pages (the mojibake was on ~16 pages beyond standards). Rule now holds site-wide.
  - **Deleted the legacy root HTML (P3-2):** 54 pre-Astro files (the source of the world-record regression). Confirmed CF serves `dist/` from `astro build`, so they were never deployed.
  - **Optimise:** added 3 FAQs to the world-record page (visible + FAQPage schema) targeting the high-impression pos 6-9 zero-click phrasings "world record for hanging on a bar" (280 impr), "longest time hanging from a bar" (163 impr), "who holds the dead hang world record" - all answered with verified Adachi 80:41. Added SpeakableSpecification (WebPage schema) to both money pages for AI/voice eligibility. All JSON-LD validated (4 blocks/page parse clean).
  - Both parked owner-decision items are now DONE. No open P3 items remain from the Fable audit.
- 2026-07-20 — **Indexation + internal-link repair pass.** Verified yesterday's sitemap/404/nav fixes are still correct (sitemap-index.xml lists exactly 53 real URLs, matches `src/pages` 1:1; no drift). Re-inspected 20 URLs via GSC batch inspection: all still show "Discovered/Crawled - currently not indexed" or "unknown to Google" (expected, Google hasn't recrawled since yesterday's fixes yet, this is a wait, not a new problem).
  - **Found and fixed 6 broken internal links** that were silently soft-404ing (now hard 404 since yesterday's `404.astro` fix, so these were actively broken and previously invisible): `forearm-exercises` linked to five non-existent sub-pages (`/equipment-reviews/grip-trainers/`, `/complementary-exercises/farmers-walk/`, `/complementary-exercises/plate-pinches/`, `/complementary-exercises/wrist-curls/`, `/grip-strength/forearm-anatomy/`) and `wrist-exercises` linked to `/mobility/wrist-flexibility/`. Retargeted each to the real existing page that already covers that subtopic (verified content match first), one to a heading-id anchor (`/wrist-exercises/#strengthening`).
  - **Ranked all 51 content pages by inbound-internal-link count** to find true orphans beyond the ones already fixed yesterday. Two pages had zero internal inbound links anywhere in the repo: `training-science` and `equipment-reviews` (the latter only via the broken link above). Several more had exactly one: `athletic-performance`, `deadhang-workout`, `grip-strength-benefits`, `one-arm-hang-guide`, `weighted-hang-guide`.
  - **Added 7 new contextual internal links** from the site's 3 highest-traffic pages to these thin/orphaned pages (natural anchors, verified the target page actually covers the linked topic before adding, no dashes, no invented stats): from `/dead-hang-time-by-age/` (1,129 clicks/28d) to `/grip-strength-benefits/` and `/training-science/`; from `/dead-hang-world-record/` (142 clicks) to `/athletic-performance/`; from `/deadhang-guide/` (22 clicks, hub page) to `/one-arm-hang-guide/`, `/weighted-hang-guide/`, `/training-science/`, `/equipment-reviews/` and `/deadhang-workout/`. Updated `deadhang-guide`'s stale `dateModified` (was frozen at 2026-03-03) to 2026-07-20 since content materially changed.
  - Confirmed no `noindex`, no `Disallow`, sitemap URL count matches source count, robots.txt correct. `npm run build` passes (54 pages incl. 404). Committed and pushed to `sunnyp81/deadhangs-com`; Cloudflare Pages auto-deploys on push (separate CF account, see warning above). Resubmitted `sitemap-index.xml` to GSC as a freshness ping (content unchanged, still 53 URLs).
  - **Next highest-ROI action: this is now purely a waiting + backlink problem, not a technical one.** All plumbing (sitemap accuracy, 404 handling, nav/footer, orphan linking) is fixed. Indexation will not move further from on-page or internal-link work alone; it needs (1) time for Google to recrawl with the new signals, and (2) external backlinks/authority (per the 07-19 Fable red-team finding, on-page edits move CTR at current position but not position 7 → 4, and indexation of the 45%+ never-crawled pages needs authority signals crawl budget alone won't fix). Check batch URL inspection again in 1-2 weeks; if still "Discovered - not indexed" after a real recrawl, the bottleneck is confirmed to be domain authority, not architecture, and the highest-ROI move becomes link-building to the two money pages (the world-record women's-record correction is the strongest outreach hook per the 07-19 note).
- 2026-07-19 — **Fable-audited CTR + correctness overhaul, shipped LIVE** (commits 6421371 + df23802). Fable (fable model) ran a full audit; Opus fixed the correctness-critical page, 3 Sonnet subs did the parallel work; all deployed + verified live.
  - 🔴 **ROOT CAUSE of the world-record page's 0.71% CTR (50k impr, pos 7): the page stated a SUPERSEDED record.** Title/meta/schema/body/tables/SVG all showed "Harald Riise 16:03" (his 2021 mark). The real current Guinness "longest duration in the dead hang position" is **Kenta Adachi (Japan) 80 min 41 sec, 18 Nov 2022** (verified via GWR + Stars&Stripes; Adachi also holds most-consecutive-pullups 651). Riise 16:03 (2021) and Tazio Gavioli 13:52 (2018) are prior holders. This was a May-9-migration regression (old pre-correction HTML carried forward) — the exact risk the Apr-1 fact correction had already fixed once. **The legacy pre-Astro HTML still sits at repo root (`dead-hang-world-record/index.html` etc, ~45 dirs) and is the booby-trap source — NOT deployed (CF builds dist/), flagged for cleanup (P3-2).**
  - World-record page fully rewritten: correct Adachi record, honest 3-holder progression table, **removed the unverified women's record** ("Ángeles López Soto 12:21" could not be verified anywhere except this site itself — circular; also dropped invented "Unknown competitor"/"Previous holders" rows), rebuilt timeline SVG (3 real points), recomputed "% of world record" vs 4841s, fixed dead `/#calculator` CTA → `/dead-hang-calculator/`, added og:image, purged dashes, dates → 2026-07-19. New title: "Dead Hang World Record: 80 Min 41 Sec (Kenta Adachi)".
  - time-by-age (top page, 101k impr): CTR title → "Dead Hang Time by Age and Gender: Average Times Chart"; numbers-first meta; first-screen snippet answer; new "how long can the average person hang from a bar" FAQ; aligned FAQ schema to visible; dropped uncited "2018 meta-analysis"; softened Attia attribution; og:image; dash purge; dates.
  - dead-hang-standards: retargeted to the "good dead hang time" / "is X seconds good" cluster (was ~0 clicks). New title "What Is a Good Dead Hang Time? Standards by Level", quick-verdict table (20s-5min), 4 "is X good" FAQs, cross-links. Also fixed 22 pre-existing mojibake sequences (â€/Â· from an old encoding round-trip) via a code-point Node script.
  - Internal linking (whole site, 53 pages): Footer had all `href="#"` dead links, Nav linked non-existent homepage anchors on all 52 article pages, homepage Quick Links never linked its two money pages. Fixed all: real links money-pages-first, Nav → real pages, "Start" CTA → /training-programs/, homepage brand title "DeadHangs.com: Dead Hang Training, Times and Records", footer year 2025→2026.
  - Sitemap: deleted stale hand-written `public/sitemap.xml` (lastmod frozen 2026-03-03) that shadowed @astrojs/sitemap; robots.txt now points to `sitemap-index.xml` (duplicate User-agent:* block removed). Resubmitted sitemap to GSC + Bing; submitted the 4 changed URLs to Bing for fast recrawl.
  - **OPEN (owner decisions):** (1) P3-1 fabricated-authority: every page's author box claims "NSCA-CSCS & NASM-CPT Certified... certified personal trainers and physical therapists with 10+ years" for an anonymous "DeadHangs Team" — same class of fabricated authority the May-9 purge targeted, survived on all 52 pages. Needs a real named credential OR honest softening (sitewide, Sunny's call — not touched). Also uncited stats remain on grip-strength ("4-week study... 25%") and deadhang-guide ("40-60% disc pressure"). (2) P3-2 delete the legacy root HTML. **Check CTR delta on both money pages ~Aug 2** (world-record especially — target 0.71% → 2.5%+ = ~+900 clicks/90d).
- 2026-07-10 — GA4 install task (growth plan action, week 0): confirmed no analytics tag anywhere in `src` (grep for `gtag`, `googletagmanager`, `G-` measurement ID pattern across the whole repo returns zero matches). GA4 property 530351656 ("deadhangs.com") exists under GCP account 387590201, confirmed via `mcp__ga4__get_property_details` and `get_account_summaries`, but neither that tool nor `master-builds.md` nor any `claude-memory` file records an actual G-XXXXXXX measurement ID (the Admin API property resource doesn't expose data streams, and no tool here reads them). **Blocked**: Sunny needs to open GA4 Admin > Data Streams > Web for property 530351656, copy the Measurement ID (create the web stream first if none exists), and hand it over. No code was changed this session.
- Correction to the growth plan's Base.astro-only instruction: only the homepage imports `src/layouts/Base.astro`; all 52 other pages (including both money pages, `/dead-hang-time-by-age/` and `/dead-hang-world-record/`) import `src/layouts/Article.astro`, which has its own independent `<head>` and does not extend Base. **The GA4 script must be added to both `Base.astro` and `Article.astro`**, or it will only ever measure the homepage's ~13 clicks/90d and miss the ~2,253 clicks/90d driving the site's growth. Article.astro already has a cookie-consent banner (`localStorage` key `cookieConsent`, values `accepted`/`declined`); Base.astro has no such banner. Whoever installs the tag next should gate `gtag('config', ...)` on `cookieConsent === 'accepted'` in Article.astro to match, and decide whether Base.astro (homepage only) needs its own consent gate or can fire unconditionally given its low traffic and the site's existing analytics disclosure in `/privacy/`.
- 2026-07-10 (later same day) — **GA4 installed.** Measurement ID `G-YBPRQVR2Y3` (property 530351656) confirmed via the Analytics Admin API dataStreams endpoint (previous session's blocker resolved). Added a consent-gated loader (`window.__gaLoad`, defines `dataLayer`/`gtag`, injects `gtag/js?id=G-YBPRQVR2Y3` script tag, fires `gtag('config', ...)`) to the `<head>` of both `src/layouts/Base.astro` (homepage) and `src/layouts/Article.astro` (all 52 other pages), per the correction above. It only fires if `localStorage.getItem('cookieConsent') === 'accepted'`; Article.astro's existing Accept button now also calls `window.__gaLoad()` immediately on click so consent-day visitors are tracked without a reload. Base.astro has no consent banner of its own, so the homepage only tracks visitors whose consent was already accepted on a prior visit to an article page (shared localStorage per origin) — a deliberate scope call given homepage's ~13 clicks/90d; adding a banner to Base.astro is a UI/component change out of this task's scope, flagged here as a possible follow-up. `npm run build` passed (53 pages); confirmed `G-YBPRQVR2Y3` present in both `dist/index.html` and `dist/dead-hang-time-by-age/index.html`. Committed and pushed to `sunnyp81/deadhangs-com`; Cloudflare Pages auto-deploys on push. Marking events as GA4 "key events" in the GA4 UI is a Sunny step (not done here).
